<h1>This is My Miniblog!</h1>

<%= render 'form' %>

<ul class="msg-list" >
<% @messages.reverse.each do |message| %>
  <li>
	<%= message.body %>
	<div class="timestamp">
		<%=time_ago_in_words(message.created_at)%> ago
	</div>
  </li>
<% end %>
</ul>

<br />